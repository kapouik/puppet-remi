HOSTS:
  centos9:
    platform: el-9-x86_64
    image: centos:9
    hypervisor: docker
    docker_cmd: '["/sbin/init"]'
    docker_image_commands:
      - 'dnf -y install gcc tar systemd-container initscripts'
CONFIG:
  type: foss
  masterless: true
  # log_level: debug
